const buttonsText = require('./buttons-text.enum');
const replyMessages = require('./reply-messages.enum');
const systemInfo = require('./system-info.enum');

module.exports = {
    buttonsText,
    replyMessages,
    systemInfo,
};
