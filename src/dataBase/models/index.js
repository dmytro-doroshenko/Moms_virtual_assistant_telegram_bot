const botInfoModel = require('./bot-info.model');
const doctorModel = require('./doctor.model');
const userModel = require('./user.model');
const workingScheduleModel = require('./working-schedule.model');

module.exports = {
    botInfoModel,
    doctorModel,
    userModel,
    workingScheduleModel
};
